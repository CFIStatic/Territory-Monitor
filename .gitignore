node_modules/
dist/
.cache/

# All derived — regenerated by `npm run build:data`, which `npm run dev` runs first.
data/generated/
public/data/

# Large optional datasets pulled by the fetch scripts (hundreds of MB).
data/zip-metrics.json
data/zcta.geojson

.DS_Store
*.log
.env
.env.local
